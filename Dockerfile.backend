# Backend Dockerfile for EOS3 Flow
FROM node:18-alpine

WORKDIR /app

# 复制package.json文件
COPY package.json package-lock.json* ./

# 安装依赖
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 8085

# 设置环境变量
ENV HOST=0.0.0.0
ENV PORT=8085

# 启动后端服务器
CMD ["npm", "run", "server:dev"]