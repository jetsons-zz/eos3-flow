# Frontend Dockerfile for EOS3 Flow
FROM node:18-alpine

WORKDIR /app

# 复制package.json文件
COPY package.json package-lock.json* ./

# 安装依赖
RUN npm install

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 设置环境变量
ENV HOST=0.0.0.0
ENV PORT=3000

# 启动开发服务器
CMD ["npm", "run", "client:dev"]